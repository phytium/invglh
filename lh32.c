void loop_fun(int i,int j)
{       int x, y;
        x = 0; y = 0;
       assume(i==0 && j==0);
       while (*){ 
            x++; y++; i+=x; j+=y;
            if(j>100)
            j=j+1;    
        }
}
